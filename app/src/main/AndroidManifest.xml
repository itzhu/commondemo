<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.itzhu.test.common">

    <application
        android:name="me.itzhu.test.common.DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppStyle">
        <activity android:name="me.itzhu.test.common.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="me.itzhu.test.common.EditTextActivity" />
        <activity android:name="me.itzhu.test.common.RefreshActivity" />
        <activity android:name="me.itzhu.test.common.PullTestActivity" />
        <activity android:name="me.itzhu.test.common.MenuTestActivity" />
    </application>

</manifest>